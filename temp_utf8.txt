{"step_index":662,"source":"USER_EXPLICIT","type":"USER_INPUT","status":"DONE","created_at":"2026-08-23T09:40:31Z","content":"<USER_REQUEST>\n# Task: Add a Separate \"Parent Category\" Module (Own Collection, Flat/Top-Level Only)\r\n\r\n## Context\r\nThe app has an admin dashboard at `/admin/catalog` showing catalog modules as cards: **Products** (245 items), **Categories** (18 items), **Collections** (6 items), **Brands** (12 items), and more below (Attributes, Inventory, Warehouses, Price Rules).\r\n\r\nThe existing **Categories** module already supports deep, self-referencing hierarchy via `parentId` (e.g. `Living Room → Sofa Set → Fabric Sofa`). This works well and should NOT be changed.\r\n\r\n## Goal\r\nAdd a **new, separate \"Parent Category\" module** — its own collection/table, completely distinct from the existing Categories collection. This will represent a small, flat, top-level set of broad divisions (e.g. \"Furniture,\" \"Lighting,\" \"Decor\") that sit **above** the existing Category hierarchy. Every top-level Category (one with `parentId: null`, e.g. \"Living Room,\" \"Bedroom\") will now additionally reference which Parent Category it belongs to.\r\n\r\nThis keeps things simple:\r\n- **Parent Category** = flat list only, no nesting within itself. Just broad divisions.\r\n- **Category** = keeps its existing deep, self-referencing hierarchy exactly as it works today, unchanged.\r\n- Only the **top-level Categories** (the ones that currently have `parentId: null`) get a new required link to a Parent Category.\r\n\r\n## Required changes\r\n\r\n### 1. New \"Parent Category\" card on `/admin/catalog`\r\nAdd a new card to the Catalog Modules grid, positioned **immediately after the \"Categories\" card** (so the order becomes: Products, Categories, Parent Category, Collections, Brands, ...).\r\n\r\nMatch the exact visual style of the existing cards (icon in a rounded square, item count pill top-right, bold title, short description, \"MANAGE ...\" link at the bottom):\r\n```\r\nIcon: use a suitable icon (e.g. a layers/stack icon, distinct from the folder icon already used for Categories)\r\nCount: dynamic, e.g. \"2 Items\" (pulled from however many Parent Categories exist)\r\nTitle: Parent Category\r\nDescription: \"Top-level divisions that group your categories (e.g., Furniture, Lighting).\"\r\nLink: MANAGE PARENT CATEGORIES\r\n```\r\n\r\n### 2. New data model: `ParentCategory` (separate collection/table from `Category`)\r\n```\r\nParentCategory\r\n{\r\n  id,\r\n  name: String (required),\r\n  slug: String (required, unique),\r\n  description: String (optional),\r\n  status: String (enum: ['draft', 'published'], default: 'draft'),\r\n  displayOrder: Number,\r\n  createdAt: Date (auto),\r\n  updatedAt: Date (auto),\r\n}\r\n```\r\nNo `parentId` field on this model — Parent Category is intentionally flat, single-level only, no self-nesting.\r\n\r\n### 3. Update the `Category` model\r\nAdd a new field to the existing `Category` model:\r\n```\r\nCategory\r\n{\r\n  ...existing fields (name, slug, parentId, description, status, media, display settings, SEO, etc. — unchanged),\r\n  parentCategoryId: ObjectId (ref: 'ParentCategory') \r\n    // REQUIRED only when parentId is null (i.e. this is a top-level Category)\r\n    // Should be null/not applicable when parentId is set (i.e. this Category is nested under another Category — it inherits its Parent Category association through its ancestor chain, no need to duplicate it on every level)\r\n}\r\n```\r\nValidation rule: a Category can only set `parentCategoryId` if `parentId` is null. If a Category already has a `parentId` (it's nested under another Category), `parentCategoryId` should stay empty/unused for that record — the top of its ancestor chain already carries that link.\r\n\r\n### 4. \"Manage Parent Categories\" page (`/admin/catalog/parent-categories` or similar route, match existing routing convention)\r\nA simple CRUD page, much simpler than the full Category form (no need for the 4-step Basic Info / Media & Banner / Display Settings / SEO & Publishing wizard — Parent Category doesn't need that much):\r\n- List view: table of existing Parent Categories (Name, Slug, Status, Category count, Actions)\r\n- Create/Edit form: just Name, Slug (auto-suggested from name, editable), Description (optional), Status (draft/published toggle)\r\n- Delete: block deletion if any Category currently references this Parent Category — show a clear error listing how many/which categories depend on it, so the admin can reassign them first.\r\n\r\n### 5. Update the existing Category creation/edit form\r\nOn the \"Basic Information\" step of the Category form, **only when \"Parent Category\" (the existing nested-category dropdown, i.e. `parentId`) is set to \"None (Top Level)\"**, show an additional required dropdown:\r\n```\r\nParent Category Group *   [dropdown, pulls from the new ParentCategory collection, e.g. Furniture / Lighting]\r\n```\r\n(Naming note: since \"Parent Category\" is already used as the label for the existing `parentId` dropdown in the current form, rename that field's label in the UI to **\"Parent (within Categories)\"** or similar, and use **\"Parent Category Group\"** or **\"Division\"** for this new field, to avoid confusing the two different concepts in the UI. Confirm final wording with the team, but they must be visually and semantically distinct.)\r\n\r\nIf the Category being created/edited is nested (its `parentId` is set to some other Category), hide this new dropdown entirely — it's not applicable at that level, since it inherits from its ancestor.\r\n\r\n### 6. API routes\r\n- `GET /api/parent-categories` — list all (support `?status=published`)\r\n- `POST /api/parent-categories` — create\r\n- `PUT /api/parent-categories/:id` — update\r\n- `DELETE /api/parent-categories/:id` — delete, with the dependency-check safety noted in step 4\r\n- Update `GET /api/categories` (or wherever categories are fetched) to optionally populate/include the linked Parent Category name for top-level categories, useful for breadcrumbs and the Navbar Builder.\r\n\r\n### 7. Navbar Builder breadcrumb update\r\nThe Group picker's breadcrumb display (e.g. `Furniture > Living Room > Sofa Set`) should now be built by walking: **Parent Category name → then up the Category's own `parentId` ancestor chain → down to the selected category**. Example: if \"Living Room\" (top-level Category) belongs to Parent Category \"Furniture,\" and \"Sofa Set\" is nested under \"Living Room,\" the breadcrumb reads `Furniture > Living Room > Sofa Set` exactly as before — visually unchanged, just now the first segment comes from the new ParentCategory collection instead of being just another Category record.\r\n\r\n### 8. Seed data\r\nSeed two Parent Categories for testing:\r\n```json\r\n[\r\n  { \"name\": \"Furniture\", \"slug\": \"furniture\", \"status\": \"published\" },\r\n  { \"name\": \"Lighting\", \"slug\": \"lighting\", \"status\": \"published\" }\r\n]\r\n```\r\nUpdate the existing seeded top-level Categories (\"Furniture\" and \"Lighting\" as Categories, if they currently exist as such) — since \"Furniture\" and \"Lighting\" now become Parent Categories instead, migrate the existing seed data: what was previously `Category: \"Furniture\" (parentId: null)` should now become `ParentCategory: \"Furniture\"`, and what was under it (e.g. \"Living Room,\" \"Bedroom\") becomes top-level Categories (`parentId: null`) with `parentCategoryId` pointing to the new \"Furniture\" Parent Category record.\r\n\r\n## Acceptance criteria\r\n- [ ] New \"Parent Category\" card appears on `/admin/catalog`, positioned right after the \"Categories\" card, matching the existing card style and showing a live item count.\r\n- [ ] \"Manage Parent Categories\" page supports full CRUD (Name, Slug, Description, Status) — no multi-step wizard needed, kept simple.\r\n- [ ] Parent Category cannot be nested within itself (flat list, no `parentId` field on this model).\r\n- [ ] Category form shows the new \"Parent Category Group\" dropdown only when the Category being created is top-level (`parentId: null`); hidden for nested categories.\r\n- [ ] A top-level Category cannot be saved without selecting a Parent Category Group (required field).\r\n- [ ] Deleting a Parent Category that's still referenced by any Category is blocked with a clear error.\r\n- [ ] Navbar Builder's Group picker breadcrumb correctly shows `Parent Category Name > Top-level Category > ... > Selected Category`.\r\n- [ ] Seed data reflects the migrated structure: \"Furniture\" and \"Lighting\" as Parent Categories; \"Living Room,\" \"Bedroom\" etc. as top-level Categories under \"Furniture.\"\r\n\r\n## Out of scope\r\n- Nesting within Parent Category itself (explicitly flat, single level only).\r\n- Changing the existing deep Category hierarchy logic — that stays exactly as it currently works, just gains one new optional/conditional field.\r\n- Bulk import/migration tooling — a simple one-time seed script covering the example data above is enough for now. but remeber one thik parent category inside ui crete like category page but field add which are necessary i already mentioned in this neccessary field\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-08-23T15:40:31+06:00.\n\nThe user's current state is as follows:\nOther open documents:\n- c:\\Users\\Doryf\\Downloads\\premium-furniture-ecommerce\\src\\admin\\pages\\finance\\reconciliation\\ReconciliationCenter.jsx (LANGUAGE_JAVASCRIPT)\n- c:\\Users\\Doryf\\Downloads\\premium-furniture-ecommerce\\src\\components\\commerce\\products\\presentation\\Product360Viewer.jsx (LANGUAGE_JAVASCRIPT)\n- c:\\Users\\Doryf\\Downloads\\premium-furniture-ecommerce\\src\\admin\\context\\marketing\\MarketingContext.jsx (LANGUAGE_JAVASCRIPT)\n- c:\\Users\\Doryf\\Downloads\\premium-furniture-ecommerce\\src\\admin\\pages\\commerce\\customers\\segments\\SegmentBuilder.jsx (LANGUAGE_JAVASCRIPT)\n- c:\\Users\\Doryf\\Downloads\\premium-furniture-ecommerce\\src\\admin\\pages\\customers\\CustomerDashboard.jsx (LANGUAGE_JAVASCRIPT)\nRunning terminal commands:\n- npm run dev (in c:\\Users\\Doryf\\Downloads\\premium-furniture-ecommerce, running for 2h40m51s)\n</ADDITIONAL_METADATA>"}
